const request = require('supertest');
const CreatePoll = require('../client/pages/Login.jsx')
const server = 'http://localhost:3000';

//need to click the button and see if we are correctly routed to the login page
